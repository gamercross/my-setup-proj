# 🗒️ 작업 로그

날짜별로 **요약**(사람·Claude 가 작성)과 **커밋**(git 이력에서 자동)을 모읍니다.

- 매 턴 종료 시(Stop 훅) 전체 이력에서 다시 만들어집니다. `<!-- SUMMARY:날짜 -->` 블록은 지우지 않습니다.
- 요약은 각 작업 세션 후 갱신합니다 (finisher 단계 또는 "작업로그 정리" 요청 시).
- 매일 23:50(launchd `com.aicomputeros.worklog`)에 그날 요약이 커밋·푸시되고 슬랙에 전송됩니다.

주간 계획·체크리스트는 [PROGRESS.md](docs/progress/PROGRESS.md) 를 보세요.

<!-- BEGIN LOG -->

## 2026-09-07

### 📝 요약
<!-- SUMMARY:2026-09-07 -->
### 오늘 한 일
- **Phase D2-a 개발 (파이프라인)** — Claude 재시도 백오프 + `sync_logs` 기록 + 조회 API (`feature/d2a-sync-logs-retry`, `feature/d1-agent-db` 위 스택 → `origin/main` 리베이스). 에이전트: `agent/services/retry.py`(`call_with_retry` — 3회 시도/재시도 2회/1·2s 지수 백오프, 인증·4xx 즉시 실패), `agent/services/claude.py`(`ask()` 재시도 적용 + `Anthropic(timeout=30, max_retries=0)`), `agent/db.py`(`log_sync()` — `sync_logs` 기록, 예외 안 냄), `agent/daily_brief.py`(`_run()` 에서 `ensure_schema` 배선 — D1 이월). 백엔드: `GET /api/sync/logs`(읽기 전용) + `db.getSyncLogs`. 테스트: TC-AGENT-16~19(`test_retry.py` 신규), TC-SYNC-06~10(`sync.test.js` 신규).
- 커버: FR-AGENT-06 AC-3 ✅, NFR-REL-05 🚧(Claude 호출만 — Gmail·Calendar·Notion 재시도는 D2-b), NFR-OBS-03 🚧(기록·조회만), FR-SYNC-03 🚧(조회 API). FR-CAL-03 "D2 이월" → "D2-b 이월" 표기 정정.
- 문서 7종 갱신: CROSSCUTTING, DESIGN, API_REFERENCE, AGENT, REQUIREMENTS_NONFUNCTIONAL, TRACEABILITY, TEST_PLAN. TEST_PLAN §3.5d 표 컬럼명을 실제 `sync_logs` 스키마(`id/service/status/last_sync/error_message`)에 맞춰 정정.
- 검증: `agent pytest -m "not network"` 18/0 (2 deselected), `verify.sh` 27/0/0 (`check-docs.sh` 11/0/0 포함), `backend npm test` 59/0.
- **감사 후속 정리 마무리 (`fix/ai-results-cleanup`)** — supervisor PASS(1차, 회귀 0). C1 NFR-REL-03 안전 종료(`backend/src/lifecycle.js` 신규 + `server.js`·`db/index.js` 배선 + `lifecycle.test.js` TC-REL-01~06), C2 NFR-MAINT-02 서비스 계층(`services/{tasks,projects}.js` + `errors.js` + 얇은 라우트 + `services.test.js` TC-MAINT-01~05), B 다이어그램·수치 정합(ARCHITECTURE·DATA_ARCHITECTURE·DESIGN·RUNTIME_VIEW·AS_IS·ai_결과값.md). NFR-REL-03 요구 문구 '프로세스 유지'→'로깅 후 안전 종료' 개정(ADR-0016 근거).
- 후속 4건 반영: TEST_PLAN pytest 54 passed 정정, AS_IS agent 테스트 54케이스 정정, AS_IS 다이어그램에 `sync 읽기 전용 직접 조회` 점선 + TRACEABILITY NFR-MAINT-02 예외 구절, ai_결과값.md 보안 절에 MCP 지시문 주입 한계 한 줄.
- 검증(재실행): `backend npm test` 70/0, `scripts/smoke.sh` 통과, `verify.sh` 30/0/0, `check-docs.sh` 11/0/0, `render-diagrams.sh` 실패 0, `agent pytest -m "not network"` 54 passed (4 deselected). 5개 커밋 분할 → `fix/ai-results-cleanup` 푸시 + PR(base main).
- **Phase D3 개발 (파이프라인) — Notion 저장 + Brief 조회 API·위젯 + launchd 자동 실행** (`feature/d3-brief-ui-schedule`, main `440e60a` 기준). FR-AGENT-03·04·05 ✅, FR-AGENT-06 AC-5 ✅. 커밋 5개 분할 → 푸시 + [PR #26](https://github.com/gamercross/my-setup-proj/pull/26)(base main).
  - **에이전트 Notion 저장**: `agent/services/notion.py` 전면 재작성 — `notion-client` SDK 대신 `requests` 로 REST 직접 호출(`NOTION_VERSION=2022-06-28`), 부모 페이지(`NOTION_PARENT_PAGE_ID`) 아래 새 페이지 생성 후 URL 반환, 본문 2000자 초과 시 줄바꿈 경계 우선 paragraph 블록 분할. `NotionNotConfigured`(키·부모페이지 미설정 = 실패 아닌 스킵) / `TransientNotionError`(429·5xx → `call_with_retry` 3회 1·2s 백오프) / 401·400 즉시 `RuntimeError`. `requirements.txt` 에서 `notion-client` 제거, `requests==2.34.2` 핀.
  - **토큰 마스킹 공용화**: `agent/services/sanitize.py` 신규 — `_sanitize_error`(토큰류 `***` + 500자 절단)를 `google_common` 에서 분리, `google_common` 은 재노출로 하위 호환 유지. `agent/daily_brief.py` Notion 블록 교체: 성공→`upsert_brief`(같은 date 행에 `notion_url` 만 병합, `created_at` 유지)+`log_sync('notion','success')`, 실패→`log_sync('notion','failed', <마스킹>)`, 미설정→결과에 ℹ️ 안내만.
  - **백엔드 조회 API**: `db.js` `getBriefByDate`(SELECT 전용 — briefs 는 에이전트 소유, ADR-0011) + `services/brief.js`(로컬 시각 기준 `todayString` — `toISOString()` UTC 회피) + `routes/brief.js` `GET /api/brief/today` + `routes/api.js` 배선. **빈 결과는 404 가 아니라 200 + `{ brief: null }`** — [ADR-0025](docs/product/architecture/adr/ADR-0025-brief-empty-response.md) 신규 채택(아침 스케줄 전 빈 상태는 정상 — 에러 배너 회피, 다른 조회 API 와 일관).
  - **프론트 위젯**: `store/useBriefStore.js`(`useCalendarStore` 패턴 — 액션은 throw 안 함, `loaded` 플래그로 "미조회"↔"오늘 없음" 구분) + `components/BriefCard.jsx`(순수 프레젠테이션, plain-text `pre-wrap` — 마크다운 파서·`dangerouslySetInnerHTML` 없음, NFR-SEC-04. Notion 링크는 외부 내비 차단 대응 클립보드 복사 버튼) + `widgets/views/BriefWidgetView.jsx`(스토어 구독·4상태) + brief 를 레지스트리·기본 레이아웃에 추가. `registry.js` 리팩터: 순수 메타를 `widgets/widgetMeta.js` 로 분리하고 레지스트리는 view 컴포넌트만 결합 — `node --test`(.mjs) 에서 JSX 없이 메타 검증 가능.
  - **스케줄 자동 실행**: `scripts/daily-brief-run.sh`(`agent/venv` 활성화 + `.env` 명시 로딩 — launchd 는 셸 프로파일 미로딩 → `sync.py` → `daily_brief.py`, 단계별 로그 append + 1MB 회전, sync 실패해도 브리핑 시도) + `scripts/install-dailybrief-launchd.sh`(ROOT 자동 계산 heredoc 생성, 기본 07:30, `--uninstall`, `KeepAlive` 미설정 — 실패 시 재시도 없음 AC-5) + `scripts/com.aicomputeros.dailybrief.plist`(`__REPO_ROOT__` 플레이스홀더 템플릿 — 직접 복사 금지). `.gitignore` 에 `scripts/daily-brief.log*`.
  - 테스트: `agent/tests/test_notion.py` 신규(TC-AGENT-22~28 — `requests.post` monkeypatch, 429 재시도·401 즉시실패·블록 분할·마스킹), `test_daily_brief.py` +TC-AGENT-04·29·30(실패→sync_logs, 성공→notion_url, 미설정→스킵), `backend/test/brief.test.js` 신규(TC-BRIEF-01~06), `frontend/test/registry.test.mjs` 신규(TC-BRIEF-07) + `displayConfig.test.mjs` +TC-BRIEF-08.
  - 문서: ADR-0025 신규(+DESIGN·adr/README), AGENT.md·API_REFERENCE·UI_SPEC 의 404→200/null 정정, ADR-0007 에 D3 구현 절, AUTOMATION.md §3.5 신설(+worklog install 스크립트 안내로 정정), ENV_REFERENCE·`.env.example` 에 `NOTION_PARENT_PAGE_ID`, DATA_DICTIONARY·REQUIREMENTS_FUNCTIONAL·TRACEABILITY·TEST_PLAN·RUNTIME_VIEW·ARCHITECTURE·PROGRESS·ai_결과값.md.
  - 검증: `agent pytest -m "not network"` 64/0 (4 deselected), `backend npm test` 76/0, `frontend npm test` 13/0, `verify.sh` 30/0/0(`check-docs.sh` 11/0/0 + 서비스 스모크 포함), `frontend npm run build` 성공. 푸시 CI(agent·backend·docs·frontend) 전부 통과. PR #26 CI green.
  - **로컬 실환경 검증 완료**: (1) `.env` 에 `NOTION_PARENT_PAGE_ID`(페이지 "my-setup-proj") 추가, `NOTION_API_KEY`(`ntn_…`)·Connections 는 기존 설정 유효. (2) `bash scripts/install-dailybrief-launchd.sh` 로 launchd 등록(07:30), `daily-brief-run.sh` 실행 → sync(Google 미설정으로 실패, brief 는 계속 진행) → Claude 200 → `briefs` 저장 → Notion 페이지 생성 → `GET /api/brief/today` 200(brief 반환, `notion_url` 채워짐). exit 0. ANTHROPIC_API_KEY 는 Workspace 스코프 키로 교체 필요했음 → ENV_REFERENCE 반영(커밋 `8a8fdc0`).
  - 남은 사용자 작업: 기존 대시보드 사용자는 brief 위젯을 피커로 추가하거나 레이아웃 초기화. Gmail/Calendar sync 는 D2-b(`TOKEN_ENCRYPTION_KEY` + Google 최초 로그인) 대기.
- **Phase P4 개발 (개인 생산성 OS) — T5 공통 컴포넌트** (`feature/p4-common-components`, supervisor PASS). PERSONAL_OS §7 P4 ✅.
  - 신규: `frontend/src/components/dotFill.js`(순수 — `dotFill(pct, total=20)`/`normalizeTotal`(0이하·비정수·NaN→20)/`clampPct`(NaN→0, 0~100 clamp, 정수)), `DotProgress.jsx`(점그리드 진행바, `role="progressbar"` + aria, 채움색 `var(--w-accent, var(--accent))`, `showPercent`), `StatTile.jsx`(`<StatTile label value tone?>` — tone별 숫자색, bg `--panel`/border `--border`/radius 14px/숫자 700·40px), `Chip.jsx`(`variant?`/`onClick?`/`title?` — onClick 유무로 button/span, radius `--chip-radius`), `frontend/test/dotFill.test.mjs`(TC-P4-01~05).
  - 수정: `styles.css` `:root` — `--card-radius` 10→16, `--shadow-card` 추가(light `0 1px 2px rgba(0,0,0,.04)` / dark `.32`); `WidgetFrame.jsx` wrapper 에 `boxShadow: var(--shadow-card)`; `ProjectCard.jsx` solid 진행바 → `<DotProgress label={null} pct={pct} showPercent />` (슬라이더·commitProgress·clamp 유지).
  - 커밋 3분할: `feat`(공통 컴포넌트 4개 + 테스트) / `style`(카드 토큰 v2) / `refactor`(ProjectCard → DotProgress).
  - 문서: PROGRESS(개인 생산성 OS 표·다이어그램 P4 ✅), PERSONAL_OS §7, UI_SPEC §4.1 공통 컴포넌트 계약 표 + 토큰 v2 표(`--card-radius` 16·`--shadow-card`), ADR-0027("P4 로 미루는 것" 이행 완료), TEST_PLAN §3.4h(TC-P4-01~05 ✅)·§3.9(TC-P4-M1~4 육안 ⏳)·§7. TRACEABILITY 는 P4 가 FR 무관이라 미변경.
  - 검증: `cd frontend && node --test test/` 26/0, `npm run build`·`npm run build:demo` 성공, `verify.sh --code-only` 27/0/0(`check-docs.sh` 11/0/0 포함). 컴포넌트 시각 확인(`--card-radius` 16·`--shadow-card`·DotProgress)은 로컬 GUI 수동 검증 대기(샌드박스 GUI 불가).
<!-- /SUMMARY:2026-09-07 -->

### 📦 커밋 (52)
- `f914963` docs: D1·C6 후속 드리프트 정정 — 다이어그램 목록·브랜치명·C6 완료 반영
- `428c9d8` feat(agent): Phase D2-a — Claude 재시도 백오프 + sync_logs 기록 + GET /api/sync/logs
- `9ad07fc` Merge pull request #20 from gamercross/feature/d2a-sync-logs-retry
- `b6342b4` docs: README 에 API 키 발급 절차 추가 — Google Cloud 무료 사용법 포함
- `c73b5b1` Merge pull request #21 from gamercross/docs/api-key-setup
- `0aebcf2` chore(agent): 기본 모델 Sonnet 전환 + effort:low 로 비용 절감
- `3233086` Merge pull request #22 from gamercross/chore/agent-cost-reduction
- `d1fc742` feat(agent): Phase D2-b — Google OAuth + Gmail/Calendar 실 수집
- `eeefad7` chore: Notion MCP 도구 차단 (프롬프트 주입 벡터 제거)
- `474bece` Merge pull request #23 from gamercross/feature/d2b-google-oauth
- `0cd75e2` Merge pull request #24 from gamercross/chore/disable-notion-mcp
- `23b24a8` feat(backend): uncaughtException·종료 신호 graceful shutdown (NFR-REL-03)
- `adde2d7` refactor(backend): tasks·projects service 계층 분리 (NFR-MAINT-02)
- `ed6adc2` docs: NFR-REL-03 종료 정책 개정 + 서비스 계층 + 다이어그램 정합
- `27837eb` docs: ai_결과값.md 점검 결과 갱신 + PROGRESS 감사 후속 정리 반영
- `440e60a` Merge pull request #25 from gamercross/fix/ai-results-cleanup
- `dcac74d` feat(agent): Phase D3 — Notion 저장 (requests REST 직접 호출) + sanitize 분리
- `52478f8` feat(backend): GET /api/brief/today — 빈 결과는 200 + { brief: null } (ADR-0025)
- `af40c7f` feat(frontend): 오늘 브리핑 위젯 + 레지스트리 메타 분리
- `2272ca9` feat(scripts): 일일 브리핑 launchd 자동 실행 (FR-AGENT-05)
- `1c91d80` docs: Phase D3 정합 — ADR-0025 + 404→200/null + AUTOMATION §3.5
- `d0ecdac` Merge pull request #26 from gamercross/feature/d3-brief-ui-schedule
- `8a8fdc0` docs: ENV_REFERENCE — ANTHROPIC_API_KEY 는 Workspace 스코프로 발급
- `81db319` docs: 작업로그 — D3 로컬 실환경 검증 완료 반영
- `dbb9ab1` feat(backend): 캘린더/메일 조회 API 를 실 캐시로 배선 + tasks project_id 필터
- `86c2175` feat(scripts): seed-demo.js — 데모/프로토타입용 샘플 데이터
- `59a2cb3` docs: Phase D 마무리 정합 + ENV_REFERENCE Workspace 스코프 재반영
- `e44281b` feat(frontend): 웹 데모 모드 — VITE_DEMO 목 어댑터 + GitHub Pages 배포 (ADR-0026)
- `200b236` Merge pull request #28 from gamercross/feature/web-demo
- `a7f8ccb` Merge pull request #27 from gamercross/feature/d-finish
- `0d9d8d6` docs(README): 2026-09-07 현황 + 연결 구조 다이어그램 + 최근 작업(D3·D-마무리·웹데모)
- `202119c` ci: deploy-demo 이 Pages 를 자동으로 켜도록 (enablement: true)
- `4264600` Merge pull request #29 from gamercross/docs/readme-status-diagram
- `3272aef` Merge pull request #30 from gamercross/ci/pages-auto-enable
- `d308a25` docs: 웹 데모 피드백 문서 + README "지금 보기" 섹션
- `6ba4104` Merge pull request #32 from gamercross/docs/demo-feedback
- `eb30a0a` Merge pull request #31 from gamercross/feature/d3-brief-ui-schedule
- `3638aa3` docs: 개인 생산성 OS 방향 문서 (PERSONAL_OS.md) + 진행 다이어그램 (P0)
- `6915c2f` Merge pull request #33 from gamercross/docs/personal-os-direction
- `4ed2c40` docs: PERSONAL_OS 에 T6(진행 현황 뷰) + P9 추가
- `53625ce` docs(adr): 개인 생산성 OS P1 — ADR-0027~0031 초안 (제안)
- `9fe5906` Merge pull request #34 from gamercross/docs/personal-os-t6
- `90cfb33` Merge pull request #35 from gamercross/docs/adr-personal-os-p1
- `fc35b33` feat(frontend): P3 — 라이트 테마 1차 (ADR-0027 채택)
- `7efe382` Merge pull request #36 from gamercross/feature/p3-light-theme
- `1361ca8` refactor: P3 를 최소 범위로 축소 (CONVENTIONS §1 준수)
- `4a65b3f` fix: P3 supervisor 지적 6건 반영
- `7533bf0` Merge pull request #37 from gamercross/fix/p3-scope-trim
- `87a2c31` chore: 코드를 main 에서 직접 편집 시 승인 프롬프트 훅
- `ab56567` docs(design): P2 — 개인 생산성 OS UI 기본틀 캔버스 (6 아트보드)
- `a851ff4` Merge pull request #38 from gamercross/chore/feature-pipeline-hook
- `8566f55` Merge pull request #39 from gamercross/design/p2-ui-canvas


## 2026-09-06

### 📝 요약
<!-- SUMMARY:2026-09-06 -->
### 오늘 한 일
- **UI/UX 참조 틀 도입** (`docs/ui-style-reference`, `fix/review-followups` 위 스택 — #8). 인스타 릴스 "Claude 워크스페이스 대시보드"(Cowork)를 화면 골격·컴포넌트 패턴·시각 톤 목표 틀로 채택. 신규 `docs/product/reference/UI_STYLE.md`(레이아웃 골격·패턴 P1~P6·기능→틀 매핑·안 가져오는 것·디자인 토큰 정합·열린 질문 US-1~4) + `assets/ui-style/README.md`(캡처 규약). `STUDY_GUIDE.md §2.9` 신설. `ai_결과값.md` 에 "UI/UX 참조 틀 도입 검토" 절(U1~U8 + 미결 US-1~4).
- **C3 개발 (파이프라인)** — 캘린더 위젯 + `GET /api/calendar/events` (`feature/c3-calendar`). 백엔드: `services/calendar.js`(더미 이벤트 생성, 실 Google 연동은 D2 — ADR-0006/0011), `routes/calendar.js`(`?from&to`, `from > to` 는 400 아닌 200 빈 목록), `test/calendar.test.js` TC-CAL-01~07. 프론트: `useCalendarStore.js`(zustand fetch + 날짜 배지 파생), `CalendarWidget.jsx`, `Dashboard.jsx` 3패널 배선(할일·프로젝트·캘린더). FR-CAL-01(🚧 더미)·FR-CAL-02(✅). 문서: `CAL.md` 신설, API_REFERENCE·UI_SPEC·REQUIREMENTS_FUNCTIONAL·TRACEABILITY·TEST_PLAN·requirements/README·PROGRESS·DESIGN(§1 AS-IS·§6 트리·§8 Phase C 표) 갱신. `.gitignore` 에 `making-os/` 추가.
- 검증: `npm test` 46/46, `verify.sh` 21/0/0, `npm run build` 성공. 브라우저 수동 TC-UI-17~19 는 로컬 대기(C1·C2 선례, 샌드박스 창 기동 불가).
- **C4 개발 (파이프라인)** — 다이어그램 뷰어 + `GET /api/diagrams` (`feature/c4-diagram-viewer`, C3 `0b01c05` 위 스택). **ADR-0014 제안→채택** (본문 4곳 드리프트 정정 + 구현 현황 절 추가). 백엔드: `services/diagrams.js`(의존성 없는 `fs.readdirSync` 재귀 파싱, 깊이 8·파일 500·1MB·블록 300 상한, `parseMermaidBlocks` 순수함수, `DOCS_PATH`→저장소 docs→`resourcesPath` 우선순위, docs 부재 시 200 빈 목록), `routes/diagrams.js`(`?doc=` 필터, 서비스 호출만), `test/diagrams.test.js` TC-DIAG-01~05. 프론트: `DiagramPanel.jsx`(`mermaid@11.17.2` 동적 import — 별도 청크 ~683kB, 블록 단위 렌더+원문 폴백, CSP 무완화), `Dashboard.jsx` 패널 배선. FR-UI-05 ✅ C4, G9 해소. 응답 스키마 `[{ doc, path, index, title, code }]`. electron-builder `extraResources` 실배선은 E3 이월. supervisor PASS 1회(비차단 정리 항목 — verify.sh 2줄·ADR 드리프트·TEST_PLAN 한 줄 — finisher 가 반영). 문서: DESIGN(§1·§8)·TRACEABILITY·TEST_PLAN·API_REFERENCE·UI_SPEC·UI.md·REQUIREMENTS_FUNCTIONAL·DIAGRAMS.md·adr/README 갱신.
- 검증: `npm test` 51/51, `verify.sh` 23/0/0, `npm run build` 성공(mermaid 683kB 별도 청크). 브라우저 수동 TC-UI-09 계열은 로컬 대기(샌드박스 창 기동 불가).
- **C5 개발 (파이프라인)** — 위젯 셸 · 대시보드 OS (`feature/c5-widget-shell`, C4 `feature/c4-diagram-viewer` 위 스택). **ADR-0020/0021/0022 제안→채택**, DASHBOARD_OS §8 DO-1~6 결정(DO-1 RGL 그리드 스냅, DO-2 타입당 1개, DO-3 localStorage, DO-4 프론트만 검증, DO-5 편집 토글, DO-6 다이어그램 위젯화). 신규: `frontend/src/widgets/{registry,defaultLayout,layoutStorage,themeVars}.js` + `widgets/views/{Tasks,Projects,Calendar,Diagrams}WidgetView.jsx` + `store/useLayoutStore.js` + `components/{WidgetShell,WidgetHost,WidgetFrame,WidgetPicker}.jsx`. 기존 4패널 뷰를 위젯으로 이관하고 `components/Dashboard.jsx` 삭제. `ErrorBoundary.jsx` 에 `fallback` prop 추가 — 위젯별 에러 격리. `react-grid-layout` 2.2.4 는 최상위 API 변경으로 `/legacy` 서브패스 사용, `react-resizable` 3.2.0 명시 핀. 레이아웃 `localStorage` `dashboard.layout.v1` 300ms 디바운스 영속 + 훼손 시 기본값 폴백. FR-WIDGET-01~04·07·08 ✅ C5, 05·06(테마·표시옵션)은 C6. supervisor PASS(2차 — 1차 지적 반영 후 재검토). 문서: DESIGN(§1·§6·§8)·DASHBOARD_OS·ADR-0014/0020/0021/0022·adr/README·UI_SPEC·WIDGET.md·REQUIREMENTS_FUNCTIONAL·TRACEABILITY·TEST_PLAN·AS_IS·CONVENTIONS·verify.sh(프론트 검사 주석) 갱신.
- 검증: frontend `npm run build` 성공, backend `npm test` 51/51, `verify.sh` 23/0/0(`--code-only` 16/0/0). 브라우저 수동 TC-WIDGET-01~08 은 로컬 대기(샌드박스 창 기동 불가).
- **3강의 구조 반영 (문서)** — COURSE_MAPPING 을 A(운영체제 실습)/B(AI시대 SW공학)/C(AITool기반 SW공학) 3섹션으로 재작성, 강의 태그 A-W#/B-W#/C-W# 체계(SSOT = COURSE_MAPPING §4), ROADMAP·DESIGN §8 에 3강의 주차 열, 맥락 문장 17개 파일 정정(학과명 삭제·우송대 오타), TEST_PLAN §3.8 TC-DOC-01~04. supervisor PASS 2차. 코드 변경 0.
- **Supabase 부트스트랩 (파이프라인)** — `@supabase/supabase-js` 2.115.0 정확 핀, `backend/src/supabase.js` 팩토리(지연 싱글턴, 미설정 시 `null` + 1회 경고) + `GET /api/sync/health`(3상태 `ok`/`unconfigured`/`error`, 항상 200, 비밀값 미노출), `backend/test/supabase.test.js` TC-SYNC-01~05(네트워크 미사용). ADR-0008 후속 절(동기화 아님 — 부트스트랩 ≠ 동기화, 상태 `채택` 유지). 스키마·동기화·인증·`user_id` 없음 — backend 는 Supabase 읽기/쓰기 안 함(ADR-0015). FR-SYNC-01/02 는 E2 그대로. supabase PASS 1회. 검증 56/27/11. 👤 사용자: `SUPABASE_KEY` 가 anon public 인지 확인.
- **세션 문서 감사 (파이프라인)** — 세션 대량 편집 문서의 정합·다이어그램·3강의 반영 확인 (`docs/session-audit`, PR #15 위 스택). 수정: AS_IS §2.7 다이어그램 자기모순(G3 미연결↔배선완료) 해소·다이어그램 범례에 런타임 HTTP 호출 추가·§2.1 상태관리 행에 `useCalendarStore`(C3)·`useLayoutStore`(C5) 보강, DASHBOARD_OS §5 노드명(WidgetView), ROADMAP↔DESIGN §8 표 정합, UI_SPEC §0/§2 헤딩(위젯 셸=현재), adr/README 강의열 SSOT 정합(5개 ADR: 0009·0012·0020·0021·0022), README/ONBOARDING/PROGRESS/CAL 드리프트(backend 39→56, verify→27/0/0, 12개→23개 ADR). 3강의 15주 표·강의계획서 정확도 문제 없음. supervisor PASS. 코드 변경 0. DOC_HEALTH 11/0/0, render 실패 0, `verify.sh --code-only` 19/0/0.
- **C6 개발 (파이프라인)** — 위젯 커스터마이즈 (`feature/c6-widget-customize`, main `68b1e5a` 기준). `styles.css` :root 토큰(전역 hex→var 1:1 치환, 시각 변화 0), `themeToVars` 화이트리스트(bg/accent/text/radius/density/titlebar, THEME_KEYS 고정 순회 + 색 정규식·CSS.supports 주입 방어), `themePresets` 다크·미니멀·강조(보라 accent), `WidgetSettings` createPortal 모달(테마+표시 탭, 자유 텍스트 0), `registry.configSchema` + `displayConfig.resolveDisplay`(뷰 3종 클라이언트 필터/정렬, fetch 계약 불변), number clamp 에 step 스냅 추가. TC-WIDGET-09~13. `updateConfig` 1단 병합 유지(호출측 스프레드). ADR-0022 채택→구현 완료. US-1(앰버→보라 전역)은 미결 유지 — 보라는 '강조' 프리셋 accent 에만. supervisor CHANGES 1회(순환 import·titlebar 인라인) → PASS 2차. finisher 정리 9건(F1 showEmpty visible 기준·F2 장소색 --muted·F3 step 스냅·F4 statusFilter undefined 방어 + UI_SPEC/DESIGN/adr/README/PROGRESS/ROADMAP 문구) 반영. 검증 frontend test 9/0 · backend 56/56 · verify 27/0/0 · DOC_HEALTH 11/0/0 · build OK.
- 검증: frontend `npm test` 9/0, backend `npm test` 56/56, `verify.sh` 27/0/0, `check-docs.sh` 11/0/0, `npm run build` 성공. 브라우저 수동 TC-WIDGET-14~19 는 로컬 대기(샌드박스 창 기동 불가).
- **D1 개발 (파이프라인)** — Daily Brief 에이전트 실데이터 배선 (`feature/d1-agent-db`, main `68b1e5a` 기준). 신규 `agent/db.py`(백엔드 SQLite 읽기 전용 접근 — `DATABASE_PATH`, 쿼리 헬퍼, 쓰기는 백엔드 API 경유 — ADR-0011), `agent/tests/conftest.py`·`agent/tests/test_db.py`. `agent/daily_brief.py` 더미 제거 → 실 데이터 조회 + 실 Claude 호출 + 호출 실패 격리(일부 실패해도 나머지 브리핑 진행), `agent/tests/test_daily_brief.py` 확장. FR-AGENT-01·02 ✅, FR-AGENT-06 🚧(AC-1/2 완료, AC-3 재시도 로직 D2 이월). TC-AGENT-01·02·03·06·10~14. 문서 5종: DESIGN §7·AGENT.md·TRACEABILITY·TEST_PLAN·ENV_REFERENCE. supervisor PASS. 검증 `agent pytest -m "not network"` 11/0(2 deselected)·`verify.sh` 27/0/0·backend `npm test` 56/56.
- **ai_결과값.md 세션 감사 재작성 (별도 항목)** — 개발자가 세션 감사본을 2026-09-06 기준으로 재작성. 사용자가 "그대로 포함" 결정. D1 구현과는 무관한 문서 정리로 같은 커밋에 별도 항목으로 포함.

### 참고
- FR-CAL-03(`calendar_events` 캐시 테이블 + 실 데이터)은 D2 로 이월.
- C4: electron-builder `extraResources` 로 `docs/` 실배선은 Phase E3 로 이월.
- C5: 위젯별 테마(`themeToVars` 화이트리스트)·표시 옵션·`WidgetSettings`·레이아웃 SQLite 영속은 C6 이월.
- 강조색 앰버(`#f59e0b`)→보라(`#7c6cf5`) 전환은 medium 우선순위 배지와 충돌 → 전용 색 분리 후 C6 토큰 승격에서 처리(US-1, 사용자 결정).
- 릴스 정지 캡처 5장은 저작권상 커밋 안 함 — 사용자가 `docs/product/reference/assets/ui-style/` 에 드롭하면 완성.
- 세션 말미: #7·#8·#9~#12 가 서로 다른 base 로 병합돼 있어 `integration/main-linearize` 브랜치로 전부 `main` 기준 선형화.
<!-- /SUMMARY:2026-09-06 -->

### 📦 커밋 (24)
- `e1764cf` docs: UI/UX 참조 틀 신설 — 릴스 "Claude 워크스페이스 대시보드" 를 화면 룩 기준선으로
- `3db98b5` chore: 작업로그 2026-09-06 갱신 (UI/UX 참조 틀)
- `0b01c05` feat: Phase C3 — 캘린더 위젯 + /api/calendar/events (더미 데이터, FR-CAL-01/02)
- `8d2caa0` feat: Phase C4 — 다이어그램 뷰어 (ADR-0014 채택, GET /api/diagrams + DiagramPanel, FR-UI-05)
- `b12fc84` feat: Phase C5 — 위젯 셸 (대시보드 OS, ADR-0020/0021/0022 채택, FR-WIDGET-01~04·07·08)
- `3230fc0` docs: COURSE_MAPPING 을 3개 강의(A·B·C) 구조로 재작성 + 강의 태그 체계
- `b4eee5f` Merge pull request #6 from gamercross/feature/doc-health-checks
- `3d82df3` Merge pull request #8 from gamercross/docs/ui-style-reference
- `953824c` Merge pull request #9 from gamercross/feature/c3-calendar
- `1f6090c` Merge pull request #12 from gamercross/docs/3-course-structure
- `760c2c3` merge: C4·C5·3강의 구조(#10·#11·#12) 를 main 으로
- `9b946a3` merge: 리뷰 후속(#7) + UI/UX 참조 틀(#8) 을 main 으로
- `74ddbae` fix: DESIGN §6 useLayoutStore 줄의 링크 오인식 문자열 정정 (DOC_HEALTH STRUCT-1)
- `e571021` Merge pull request #13 from gamercross/integration/main-linearize
- `eb0c975` docs: ai_결과값 세션 점검 반영 (S1~S5) + README·주석 드리프트 정정
- `5f31e82` Merge pull request #14 from gamercross/fix/ai-results-followup
- `df308c2` feat(backend): Supabase 클라이언트 부트스트랩 — 팩토리 + GET /api/sync/health
- `dc4ff32` docs: 세션 문서 감사 — 다이어그램 정합 + C3~C5·3강의 드리프트 정정
- `4e88a9a` Merge pull request #15 from gamercross/feature/supabase-bootstrap
- `68b1e5a` Merge pull request #17 from gamercross/docs/session-audit
- `b5f8727` feat(frontend): Phase C6 — 위젯 커스터마이즈 (테마·표시 옵션, FR-WIDGET-05·06)
- `8a7ddb2` Merge pull request #18 from gamercross/feature/c6-widget-customize
- `5d874ac` feat(agent): Phase D1 — agent/db.py + daily_brief 실데이터 배선 (FR-AGENT-01·02, FR-AGENT-06 부분)
- `f336213` Merge pull request #19 from gamercross/feature/d1-agent-db


## 2026-09-04

### 📝 요약
<!-- SUMMARY:2026-09-04 -->
### 오늘 한 일
- **`ai_결과값.md` 점검 12항목 처리** (`fix/review-followups`, PR #7 — `feature/doc-health-checks` 스택). **#6 서비스 검증**: `scripts/smoke.sh` 신설 — 임시 포트+임시 DB 로 backend 기동→`/api/health`→task 생성/조회/삭제 왕복(SQLite 쓰기)→정리. `verify.sh` "▶ 서비스 확인"(21/0/0) + CI `backend` 잡. TEST_PLAN §0 3층(정적·서비스·사용자흐름). **#2/#5/#11 SETUP**: `SETUP.md` 전면 개편(§0 버전표·§1 공통·§2 macOS·§3 Ubuntu/WSL·§5 포트 규칙·§6 트러블슈팅, obsolete 절·`.env` 의 Supabase 변수 제거). `setup.sh`·CI `npm install`→`npm ci`. **#3/#4 env·포트**: `ENV_REFERENCE.md` §5 신설(주체별 `.env` 로딩 차이 표, export override, 개발 포트 3000 고정). **#7/#12 시크릿·launchd**: (7은 이전 PR 에서 처리) `scripts/install-worklog-launchd.sh` 신설 — plist 경로를 현재 저장소로 자동 생성(하드코딩 제거), plist 를 템플릿으로 명시. **#1·#8·#10 은 사용자 결정/조치 필요**(DO-1~6, 브라우저 E2E, 실행 책임). `ai_결과값.md` 에 처리 현황 표 추가. `docs/{setup,testing}/README` 정합.
- **문서 정합 검사(DOC_HEALTH) 도입** (`feature/doc-health-checks`). 에이전트/사람이 우리가 작성한 시스템을 놓치지 않게, 문서 체계가 내부 일관·완결인지 자동 확인. 신규 `scripts/check_docs.py`(+`check-docs.sh` 래퍼) — STRUCT-1~3(상대링크·mermaid·폴더 README 가 모든 .md 커버), XREF-1~4(모든 ADR↔DESIGN §2·adr/README, 모든 FR-id↔TRACEABILITY, .claude 참조 경로 실재), DRIFT-1~4(하드코딩 /Users/, 구버전 모델 id, .env.example 시크릿·`= ` 공백, 폐기된 브랜치 정책 문구). `--bundle <FR-ID>` 서브커맨드는 그 요구사항의 요약행·수용기준·추적행·관련 ADR 을 한 번에 출력(착수 전 컨텍스트 모으기). `verify.sh` 에 "▶ 문서 정합 확인" 추가(13/0/0), CI `.github/workflows/test.yml` 에 `docs` 잡 추가. 신규 문서 `docs/setup/DOC_HEALTH.md`. 정합: DRIFT-4 가 `finisher.md` 의 옛 "지금은 main 직접 커밋 허용" 을 잡아 ADR-0023 대로 수정 + `git push -u`/PR 절차로 갱신. `planner.md` 에 `--bundle` 프리플라이트 단계 추가. ADR-0019 에 "문서 체계에도 적용" 후속 절. setup/README·docs/README·tests/README·adr 표 정합.
- **폴더별 README 네비게이션 체계 완성** (`feature/docs-folder-readmes`, PR #5). `docs/` 및 카테고리화한 모든 폴더에 `README.md` 신설 — `docs/`·`docs/product/`(5카테고리 허브, 읽는순서 Mermaid)·`vision/`·`requirements/`·`architecture/`·`reference/`·`testing/`·`setup/`·`progress/`. 각 README 는 그 폴더 문서를 `무엇 / 언제 참조 / ⚠️ 놓치기 쉬운 것` 4열 표로 안내하고, 상단 네비게이션 바(⬆ 상위 · 카테고리 형제 · 🚀 ONBOARDING)로 서로 연결. `adr/README.md`·`STUDY_GUIDE.md` 에도 네비 바 추가.
- **루트 `README.md` 를 README 네비게이션 허브로 재구성** — "문서별 상세 목록"(폴더 README 와 중복) 제거하고, `폴더 README → 그 안` 라우팅 표 + README 상호링크 Mermaid + "바로 이럴 때" 열로 교체. 진입점 3개(ONBOARDING/docs/README/STUDY_GUIDE) 명시. 현재 상태 표·ADR 범위(0001~0023)·브랜치 정책([ADR-0023](docs/product/architecture/adr/ADR-0023-branch-model.md)) 최신화. 하단에 문서 탐색 네비 바.
- **`.env.example` → `.env` 시크릿 정리** — 작업 트리 `.env.example` 에 유출돼 있던 Slack Webhook URL + `=` 뒤 공백을 `git checkout` 으로 커밋된 깨끗한 버전으로 복원. 실제 URL 은 `.env`(gitignore, 커밋 안 됨) 13번째 줄에 입력. `slack-notify.sh` 테스트 exit 0, 직접 curl HTTP 200 `ok` — 알림 복구. 원인: URL 이 아무도 안 읽는 템플릿에만 있었고 `.env` 는 비어 있었음.

### 참고
- `.env.example` 은 지우지 않음 — `setup.sh` 와 문서 15곳이 "환경변수 목록의 단일 원천" 으로 참조. 값은 항상 비운다(NFR-SEC-01).
- launchd `com.aicomputeros.worklog` 는 옛 경로 가리켜 `exit 127` — plist 재설치 필요 (미처리).
<!-- /SUMMARY:2026-09-04 -->

### 📦 커밋 (5)
- `897a8c2` docs: 폴더별 README 네비게이션 체계 (에이전트·사람이 빠르게 훑고 이동)
- `2d9f89c` docs: 루트 README 를 폴더 README 네비게이션 허브로 재구성
- `71cc63e` Merge pull request #5 from gamercross/feature/docs-folder-readmes
- `a40a84d` test: 문서 정합 검사(DOC_HEALTH) — 링크·ADR표·FR추적·README 커버리지·드리프트
- `e8557de` fix: ai_결과값.md 점검 항목 처리 — 서비스 스모크·SETUP OS 분리·env/포트 규칙


## 2026-09-03

### 📝 요약
<!-- SUMMARY:2026-09-03 -->
### 오늘 한 일
- **폴더별 README 네비게이션 체계** (`feature/dashboard-os-widgets` 브랜치, PR #4). 카테고리화한 폴더마다 `README.md` 신설 — `docs/`(최상위 허브)·`docs/product/`(5카테고리 허브, 읽는순서 Mermaid)·`vision/`·`requirements/`·`architecture/`·`reference/`·`testing/`·`setup/`·`progress/`. 각 README 는 그 폴더 문서를 `무엇 / 언제 참조 / ⚠️ 놓치기 쉬운 것` 4열 표로 안내(에이전트·사람이 훑고 바로 필요한 문서로). 모든 README 상단에 네비게이션 바(⬆ 상위 · 카테고리 형제 · 🚀 ONBOARDING) — 서로 연결돼 이동경로가 보임. `adr/README.md` 에 네비 바·상태 요약 추가, ONBOARDING 문서지도 표·루트 README 문서지도에 폴더 README 링크. broken link 0.
- **브랜치 모델 정합 — [ADR-0023](docs/product/architecture/adr/ADR-0023-branch-model.md) 채택** (`feature/dashboard-os-widgets` 브랜치, PR #4 에 포함). `ARCHITECTURE.md §버전 관리` 가 실제로 쓰지 않는 `develop` 브랜치 Git Flow 다이어그램을 담고 있어 GIT_WORKFLOW.md 와 불일치했다. 실제 흐름(`feature/* → PR → main`, PR #1~#4)으로 gitGraph 교체, ADR-0023 로 "Git Flow·`develop` 미채택" 명시. `ONBOARDING`·`CONVENTIONS §6`·`GIT_WORKFLOW §2`·§7 체크리스트의 "초기엔 main 직접 커밋 허용" 문구를 "feature 브랜치 + PR 이 기본" 으로 통일. adr/README·DESIGN §2 표에 0023 추가.
- **대시보드 OS(위젯 셸) 방향 추가 — 분석·요구사항·설계·다이어그램** (`feature/dashboard-os-widgets` 브랜치). 고정 패널 → 각 데이터가 위젯으로 움직이고 위젯마다 디자인하는 데스크톱 셸로 확장. 신규: `vision/DASHBOARD_OS.md`(개념·벤치마크 6종·위젯 모델·config 스키마·범위·공부 목록·열린질문 DO-1~6), `requirements/WIDGET.md`(FR-WIDGET-01~08 Given/When/Then), ADR **0020**(위젯 셸 — react-grid-layout + 위젯 계약)·**0021**(레이아웃 영속화 — localStorage→SQLite `widget_instances`)·**0022**(위젯별 테마 — 스코프된 CSS 변수 화이트리스트) 전부 **제안**. 갱신: VISION(한 줄 정의·큰 그림 다이어그램·완료 기준), FR 요약표 §9 + 도메인 지도, UI_SPEC(§0 컴포넌트 트리·§2 와이어프레임 위젯 셸 버전·§3.8~3.10·컴포넌트 계약·`useLayoutStore`), DESIGN(§2·§3 TO-BE 다이어그램·§6 위젯 계층+시퀀스·§8 Phase C5·C6), ARCHITECTURE(프론트 스택·§1), DATA_ARCHITECTURE(분류·소유권·수명주기 다이어그램), DATA_DICTIONARY §7, GLOSSARY(위젯 용어 8), ROADMAP(C5·C6), TRACEABILITY(FR-WIDGET·ADR·결정), ARCHITECTURE_DRIVERS(ASR-8·QAS-7/8·FF-8/9·드라이버 다이어그램), ONBOARDING·STUDY_GUIDE·README, `schema.sql`(주석 처리된 `widget_instances` DDL). 코드 미구현 — 착수 전 DO-1~6 사용자 결정. main 직접 커밋 금지 규칙(GIT_WORKFLOW/CONVENTIONS/ONBOARDING)은 그대로 두고 브랜치로 작업·푸시.
- **`docs/product/` 카테고리화 + 문서 다이어그램** — 22개 평면 파일을 `vision/`(VISION·USE_SCENARIOS·AS_IS·CONSTRAINTS·RISKS) · `requirements/`(FR·NFR·TRACEABILITY + 기존 TASK/UI/AGENT/PROJ) · `architecture/`(ARCHITECTURE·DESIGN·DRIVERS·RUNTIME_VIEW·DATA_ARCHITECTURE·CROSSCUTTING·EVOLUTION + `adr/`) · `reference/`(API_REFERENCE·UI_SPEC·DATA_DICTIONARY·GLOSSARY) · `testing/`(TEST_PLAN) 로 `git mv`. ROADMAP·DOC_PLAN 은 product/ 루트 유지, setup/·progress/ 불변. 링크 250여 개를 스크립트로 재작성(3-pass), 최종 broken link 0. README 문서지도·ONBOARDING·.claude/agents·commands 정합. **다이어그램 추가**: ONBOARDING 에 폴더 문서지도(마스터), VISION(큰 그림)·FR(도메인 지도)·NFR(범주→아키텍처)·TRACEABILITY(추적 사슬)·TEST_PLAN(피라미드+게이트)·CROSSCUTTING(관심사 관통)·DATA_ARCHITECTURE(수명주기)·UI_SPEC(컴포넌트 트리+렌더 상태)·RISKS(Phase 축) Mermaid 1장씩. 코드 미변경.
- **아키텍처 심화 문서 추가 (큰 틀 보강)** — "프로세스는 잡혔는데 아키텍처가 부족" 피드백 대응. 신규: `docs/STUDY_GUIDE.md`(부족 진단 + 학습 목록: C4/arc42, SA in Practice, Release It!, DDIA, local-first, STRIDE, RFC 9457), `docs/product/architecture/ARCHITECTURE_DRIVERS.md`(ASR-1~7·품질 시나리오 QAS-1~6·피트니스 함수 FF-1~7·트레이드오프 레지스터), `RUNTIME_VIEW.md`(프로세스 목록·시작/종료 순서·연결 상태 머신·미결 RT-1~5), `DATA_ARCHITECTURE.md`(데이터 분류·소유권·마이그레이션·캐시 신선도/보관·ACL·동기화 충돌·백업), `CROSSCUTTING.md`(설정 우선순위·오류 계약·로깅/상관id·복원력 타임아웃/재시도표·멱등성), `ARCHITECTURE_EVOLUTION.md`(Stage 1~3·지금 만들 이음새·다중사용자 스케치). 신규 ADR 0015(로컬 우선 스타일)·0016(프로세스 토폴로지)·0017(RFC 9457 오류)·0018(스키마 마이그레이션)·0019(피트니스 함수) — 전부 **제안** 상태(착수 전 사용자 결정). ARCHITECTURE.md §0 뷰 지도 추가, DESIGN §2·ADR README·README 문서지도·ONBOARDING 정합. 코드 미변경.
- **작업로그 개편** — 날짜별 `요약 + 커밋` 구조. `worklog.sh` 전체 이력 재생성(한 턴 놓쳐도 복구), `<!-- SUMMARY:날짜 -->` 보존. EOD 슬랙에 요약 전송. finisher 가 커밋 시 요약 갱신. 09-02 backfill.
- Claude Code 자동 업데이트 실패 해결 (수동 `claude update` → 2.1.259).
- **FR-UI-05 다이어그램 뷰어 스펙 추가** — [ADR-0014](docs/product/architecture/adr/ADR-0014-dashboard-diagram-viewer.md)(제안) + 문서 14종 정합 반영: 갭 G9, `GET /api/diagrams` 명세, `DiagramPanel` UI_SPEC §3.7, TC-DIAG-01~03·TC-UI-09, ROADMAP·DESIGN §8 에 Phase **C4**(C1 CORS 선행). 코드 미착수 — Phase C4 에서 구현.
- **Phase B3 — 할일 CRUD 프론트 배선 (코드 배선 완료)** — 신규 `frontend/src/api/client.js`(fetch 래퍼+에러 정규화), `store/useTaskStore.js`(zustand, 낙관적 갱신+롤백), `components/{TaskForm,ErrorBanner,ErrorBoundary}.jsx`. `Dashboard.jsx`/`App.jsx` 스토어·헬스체크 배선(로딩/정상/빈/에러 4상태). 커버 FR-TASK-01~04·FR-UI-01·FR-UI-04·G3. 검증: `npm run build` 성공, `verify.sh` 15/0/0, backend 18/18. **브라우저 E2E(TC-UI-10~13)·GUI 수동체크 M1~M7 은 CORS(C1) 이후 / 로컬 수행 대기** (샌드박스 창 기동 불가). 미푸시.
- **Phase C2 — 프로젝트 CRUD 프론트 배선 + `tasks.project_id`** (`feature/c2-projects`, 미푸시). 신규 `backend/src/errors.js`(SQLite CHECK/NOTNULL/FK → 400 한국어 매핑, 영문 원문 비노출), `frontend/src/store/useProjectStore.js`(zustand, `useTaskStore` 패턴 복제 — 낙관적 갱신+롤백), `frontend/src/components/ProjectForm.jsx`(이름 필수·진행도 선택). `ProjectCard` 에 상태 select·삭제 버튼·진행도 슬라이더 추가 + `'hold'`→`'on_hold'` 통일(schema/GLOSSARY 정합). `Dashboard` 프로젝트 패널 4상태 배선(에러가 할일 패널 렌더를 막지 않음). `tasks.project_id` 라우트 검증(POST/PUT, 없는 id/타입오류 → 400) + API 응답 노출(ADR-0012). 커버 FR-PROJ-01/02·ADR-0012·G3(프로젝트). 검증: `npm test` 39/39, `verify.sh` 19/0/0(SKIP 없음), `npm run build` 성공. **이월:** 이름 인라인 수정 UI·`GET /api/tasks?project_id=` 필터·TaskForm 드롭다운. 브라우저 E2E(TC-UI-14~16)는 로컬 수동 확인 대기(샌드박스 창 기동 불가). 미푸시.
- **B3·C1·C2 스택 PR 생성·푸시** — 이전 세션들이 로컬 커밋만 하고 미푸시였던 것을 정리. B3 가 `main` 에 직접 커밋돼 있던 정책 위반을 `feature/b3-task-crud` 로 분리, 로컬 `main` 을 `origin/main`(`c8d9178`)으로 복원. 세 브랜치 푸시 후 스택 PR 생성 — [#1](https://github.com/gamercross/my-setup-proj/pull/1) B3→main, [#2](https://github.com/gamercross/my-setup-proj/pull/2) C1→b3, [#3](https://github.com/gamercross/my-setup-proj/pull/3) C2→c1. **CI 6런(push·PR) 전부 success.** 병합(아래→위 순, 각 병합 후 상위 PR base 를 main 으로) 과 브라우저 E2E 는 사용자 대기 — `/build-next` 는 STOP.
- **B3·C1·C2 스택 PR 병합 완료** — [#1](https://github.com/gamercross/my-setup-proj/pull/1)→[#2](https://github.com/gamercross/my-setup-proj/pull/2)→[#3](https://github.com/gamercross/my-setup-proj/pull/3) 아래→위 순서로 merge 커밋 병합, 각 병합 후 상위 PR base 를 `main` 으로 재지정. 3건 모두 main CI(`Test & Build`) success. 병합된 feature 브랜치 3개 삭제. 로컬 `main` = `origin/main`(`da1a6b8`). **브라우저 E2E(TC-UI-10~16)는 여전히 사용자 로컬 수동 확인 대기.**
- **Phase C1 — 백엔드 미들웨어 정식화** (`feature/c1-middleware`, 미푸시). 신규 `backend/src/middleware/{cors,requestLogger,errorHandler}.js` + `backend/test/middleware.test.js`(TC-MW-01~09). `app.js` 미들웨어 체인 정식화 — `requestLogger` 를 최상단에 두어 preflight·본문 파싱 실패(400/413) 요청까지 NFR-OBS-01 "모든 요청 1줄" 충족. `cors` 는 로컬 오리진 화이트리스트(`localhost:5173`/`127.0.0.1:5173`) + `Origin: null`(prod Electron `file://`) 허용 — 트레이드오프: 로컬 무인증 API 라 `null` 허용해도 노출 위험 낮음. 커버 NFR-SEC-06 ✅ / NFR-OBS-01 ✅ / NFR-MAINT-02 🚧(미들웨어 계층 분리 ✅) / NFR-SEC-07 부분. **dotenv 미도입 결정** — 백엔드 env 3개 전부 기본값, 시크릿 0, 재검토 D2. 검증: `npm test` 27/27, `verify.sh` 18/0/0 (SKIP 없음). 브라우저 E2E(TC-UI-10~13)는 로컬 수동 확인 대기.

---

## 📋 현재 상태 (에이전트 핸드오프)

> 상세·권위 문서: [ONBOARDING](docs/ONBOARDING.md) → [TRACEABILITY](docs/product/requirements/TRACEABILITY.md)(FR별 상태) → [DESIGN §8](docs/product/architecture/DESIGN.md)(Phase 표) → [ORCHESTRATION](docs/setup/ORCHESTRATION.md)(파이프라인). 이 블록은 요약이며 충돌 시 저 문서들이 맞다.

**진척:** 문서 체계 완성(DOC_PLAN 1~7 + 분석 보강). 코드 Phase **A2·A3·B1·B2·B3·C1·C2 완료** — 갭 G1·G2·G3(코드)·G4·G5·G6 해소. 테스트 backend 39 / agent 3, `verify.sh` 19/0/0. 개발 머신 node 26 / npm 11 / python 3.14. **B3·C1·C2 스택 PR #1/#2/#3 병합 완료(`da1a6b8`), main CI green** — 브라우저 E2E(TC-UI-10~16)만 사용자 로컬 수동 확인 대기.

### ✅ 결정된 것
- **ADR 0001~0012 채택** (0013 제안 상태). → [docs/product/architecture/adr/](docs/product/architecture/adr/)
  - 0002 better-sqlite3 / 0003 `schema.sql` 단일원천 / 0009 `DATABASE_PATH` 주입 / 0010 `NODE_ENV` 로 Vite dev·빌드 분기 / 0011 WAL + 쓰기주체 분리 / 0012 `tasks.project_id` FK `ON DELETE SET NULL`(스키마 반영, 라우트는 C2)
- **스키마 단일 원천:** `backend/db/schema.sql` (6테이블). 문서는 링크만.
- **파이프라인:** `/feature` (planner→developer→supervisor→finisher), `/build-next` (로드맵 자동 진행, 한 호출 3스텝, 결정 지점에서 정지). 상태 그래프 [ORCHESTRATION.md](docs/setup/ORCHESTRATION.md).
- **브랜치:** `feature/<짧은-이름>` → 푸시 → PR → `main`. `main` 직접 커밋·`develop`·Git Flow 안 씀 ([ADR-0023](docs/product/architecture/adr/ADR-0023-branch-model.md), 2026-09-03 채택 — ARCHITECTURE.md §버전관리 의 `develop` gitGraph 를 실제 흐름으로 교체).
- **CI:** node 22 / python 3.12 고정 (로컬은 상위 허용). 액션 v5.
- **커밋 게이트:** 실행돼 FAIL 난 검사 있으면 커밋 금지. SKIP 은 명시. [GIT_WORKFLOW.md](docs/setup/GIT_WORKFLOW.md).

### ❓ 안 정해진 것 (착수 전 결정 필요)
| 항목 | 언제 | 결정 주체 |
|---|---|---|
| ~~B3 전 CORS(C1) 순서~~ | 해소 — 로드맵 순서 유지, C1 별도 수행 (2026-09-03) |
| ADR-0013 대시보드 에이전트 작업 큐 | 핵심 4기능 완성 후 | 사용자 |
| ~~backend `.env` 자동 로딩(dotenv)~~ | 해소 — 미도입 결정 (C1, 2026-09-03), 재검토 D2 |
| ~~TC-DB-04 (CHECK 위반 → 400 매핑)~~ | 해소 (C2, 2026-09-03) — `backend/src/errors.js` 로 분리, TC-DB-04a~d |
| `ARCHITECTURE.md` 의 `SUPABASE_JWT_SECRET`/`DATABASE_URL` vs `.env.example` 불일치 | Week 10 | planner |
| ~~`ProjectCard` 상태값 `'hold'` → `'on_hold'`~~ | 해소 (C2, 2026-09-03) — `active`/`done`/`on_hold` 통일 |

### ▶️ 다음 할 일 (우선순위 순)
1. **(사용자/로컬)** B3·C2 브라우저 E2E — `cd backend && npm start` + `cd frontend && npm run dev` 로 할일·프로젝트 CRUD 왕복 확인 (TC-UI-10~16). 샌드박스 창 기동 불가로 미검증.
2. ~~스택 PR 병합~~ — 해소 (2026-09-03, #1→#2→#3 merge, main CI green).
3. **(finisher)** E2E·병합 확인 후 TRACEABILITY.md·DESIGN.md §8 의 B3/C1/C2 상태를 ✅ 로, "브라우저 E2E 대기" 문구 확정 갱신·커밋.
4. **C3** — `CalendarWidget` + `GET /api/calendar/events`(더미 데이터). 커버 FR-CAL-01/02. `/build-next` 가 자동 선택.
5. **C4** — 다이어그램 뷰어 (FR-UI-05, ADR-0014). C1 선행 완료.

### 파이프라인이 잡은 실전 버그 (참고)
- A3: `node --test test/` 가 CI node 22 에서 디렉터리 미탐색 → 글로브로 수정.
- B1: `wait-on tcp:127.0.0.1:5173` 이 Vite 7 IPv6-only 바인딩으로 무한대기 → `tcp:localhost`.
- B1: preload `apiBaseUrl` 동적 PORT vs CSP 하드코딩 3000 불일치 → 고정.
<!-- /SUMMARY:2026-09-03 -->

### 📦 커밋 (17)
- `647c621` feat: 작업로그를 날짜별 "요약 + 커밋" 구조로 개편
- `5cd0f80` docs: 작업로그 2026-09-03 요약 + 09-02 커밋 반영
- `c8d9178` docs: 작업로그에 현재 상태 핸드오프 정리 (결정/미결정/다음 할 일)
- `716d322` docs: FR-UI-05 다이어그램 뷰어 스펙 확정 (ADR-0014) + 정합성 정리
- `19df5b2` feat: Phase B3 — 할일 CRUD 프론트↔백엔드 배선 (G3 코드 배선 완료)
- `27bfda5` feat: Phase C1 — 백엔드 미들웨어 정식화 (cors·requestLogger·errorHandler)
- `1197038` docs: 외부 문서 일관성 검토(ai_결과값.md) 반영 — 상태 동기화·데이터 모델·날짜 정합
- `0c95f6c` feat: Phase C2 — 프로젝트 CRUD 프론트 배선 + tasks.project_id (FR-PROJ-01/02)
- `bed6775` docs: 문서 일관성 분석(ai_결과값.md) 추가 + 작업로그 핸드오프 갱신
- `52f8131` docs: 작업로그 커밋 목록에 bed6775 반영
- `ffb4df9` Merge pull request #1 from gamercross/feature/b3-task-crud
- `e4edfd3` Merge pull request #2 from gamercross/feature/c1-middleware
- `da1a6b8` Merge pull request #3 from gamercross/feature/c2-projects
- `bde87af` docs: B3·C1·C2 스택 PR 병합 완료 반영 (작업로그)
- `461e124` docs: 대시보드 OS(위젯 셸) 방향 추가 + 아키텍처 심화 + docs 카테고리화
- `5587deb` docs: 브랜치 모델 정합 — ADR-0023 (feature/* → PR → main, Git Flow 미채택)
- `e1a5f79` Merge pull request #4 from gamercross/feature/dashboard-os-widgets


## 2026-09-02

### 📝 요약
<!-- SUMMARY:2026-09-02 -->
**시스템 분석 → 설계 문서 체계 구축, 그리고 에이전트 파이프라인으로 Phase A2·A3·B1·B2 개발.**

- **문서 체계 (DOC_PLAN 1~7 완료)** — 현행 분석(AS_IS, 갭 G1~G8), 기능 요구사항 33개(FR, requirements/TASK·UI·AGENT 수용 기준), 비기능 요구사항(NFR, 위협 모델 포함), 설계(DESIGN, 데이터 모델, API 명세, Phase 계획), 참조 문서(GLOSSARY·DATA_DICTIONARY·API_REFERENCE·UI_SPEC·TEST_PLAN·TRACEABILITY), 프로세스 문서(CONVENTIONS·GIT_WORKFLOW·ENV_REFERENCE·ONBOARDING).
- **시스템 분석 보강** — CONSTRAINTS(제약·가정·규모 추정), RISKS(리스크 레지스터 R-1~R-16), USE_SCENARIOS(사용 여정). 텍스트 다이어그램을 전부 Mermaid 로 전환.
- **설계 결정** — ADR 0001~0013. 제안 상태였던 0009~0012(SQLite 위치·Vite 로드·DB 동시접근·`tasks.project_id`) 채택. `backend/db/schema.sql` 을 DDL 단일 원천으로 확정.
- **오케스트레이션** — 에이전트 파이프라인을 상태 그래프(ORCHESTRATION.md)로 설계, 로드맵 자동 진행 `/build-next` 커맨드 추가.
- **환경 (Phase A2)** — node/npm/python 설치, `setup.sh`·`verify.sh` 통과, 백엔드 실행 검증. CI 액션 v4→v5, node 20→22.
- **개발 (파이프라인으로)** — A3: 자동화 테스트 골격(backend supertest 15 + agent pytest 3, CI 연결). B1: Vite + React 마운트(`renderer.js` 제거, CSP dev/prod 분리, `NODE_ENV` 로드 분기) — **G1 해소**. B2: `db.js` 를 better-sqlite3 로 교체(시그니처 불변, WAL, `DATABASE_PATH`) — **G2 해소**.
- 파이프라인이 실전에서 환경 버그 3개를 스스로 포착·수정(node22 테스트 탐색, wait-on IPv6 바인딩, ...).
- **남은 것** — B3(할일 CRUD 프론트 배선) 전 CORS(C1) 처리 여부 결정 필요. 로컬에서 `npm run dev` 로 React 렌더 1회 확인 권장.
<!-- /SUMMARY:2026-09-02 -->

### 📦 커밋 (16)
- `d73aa2d` chore: my-setup-proj 독립 저장소로 분리
- `fc4404c` chore: 저장소 구조·경로 이관 정리
- `ca58bdb` docs: 시스템 분석·요구사항·설계 문서 + 에이전트 git 절차 추가
- `f330392` ci: GitHub Actions 액션 v4 → v5 (setup-python v6) 업그레이드
- `45e5831` docs: API_REFERENCE·UI_SPEC 추가 (DOC_PLAN 4번)
- `91f3e99` docs: DESIGN 보강 + ADR 분리 + Mermaid 다이어그램 도입 (DOC_PLAN 5번)
- `eadef03` docs: TRACEABILITY·TEST_PLAN 추가 (DOC_PLAN 6번)
- `62a5be3` docs: ONBOARDING 추가 + 에이전트 문서 갱신 + README 최신화 (DOC_PLAN 7번)
- `849bbce` docs: 시스템 분석 보강(제약·리스크·시나리오·위협모델) + 텍스트 다이어그램 Mermaid 전환
- `45f0a15` chore: 로컬 개발 환경 구축 (Phase A2)
- `595a581` docs: 에이전트 문서 정합 + 오케스트레이션 그래프 설계
- `83b4d48` docs: 제안 ADR 4건 채택 + VISION 향후 확장(에이전트 작업 큐)
- `97baf53` docs: 현재 세팅 기준 md 검수 + render-diagrams.sh bash 3.2 호환 수정
- `584e700` test: Phase A3 — 자동화 테스트 골격 + CI 연결
- `ad181a3` feat: Phase B1 — Vite 번들러 + React 마운트 (G1 해소)
- `385d14e` feat: Phase B2 — db.js 를 better-sqlite3 로 교체 (G2 해소, FR-TASK-05)


<!-- END LOG -->
