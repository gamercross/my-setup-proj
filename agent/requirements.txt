# Python 에이전트 의존성 (Week 1 기준)
# 최신 Claude 모델(claude-opus-5) 사용을 위해 최신 anthropic SDK를 사용한다.

anthropic>=0.49.0
python-dotenv>=1.0.0

# 외부 통합 (Week 4~7에서 실제 사용, 미리 설치)
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.100.0
apscheduler>=3.10.0

# Notion REST 직접 호출 (Phase D3 — notion-client SDK 대신 requests 사용)
requests==2.34.2

# 개발/테스트
pytest>=7.4.0

# ── Phase D2-b: Google OAuth + Gmail/Calendar 실 수집 ──────────────
# 재현성을 위해 정확 핀. python 3.14 venv 에서 검증됨.
cryptography==50.0.1
google-api-python-client==2.200.0
google-api-core==2.34.0
google-auth==2.57.0
google-auth-httplib2==0.4.2
google-auth-oauthlib==1.4.1
googleapis-common-protos==1.75.2
httplib2==0.32.0
oauthlib==3.3.1
protobuf==7.36.1
pyasn1==0.6.4
pyasn1_modules==0.4.2
requests-oauthlib==2.0.0
uritemplate==4.2.0
